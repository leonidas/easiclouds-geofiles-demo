#cloudmap
  leaflet(height='100%', center='europeCenter', markers='markers', paths='paths', legend='legend')

#filters
  #leologo
  slider-filter(title='Memory', min=0.5, step=0.5, max=32, val='filterSelection.memory', unit='Gb', class='memory')
  slider-filter(title='CPU cores', min=1, step=1, max=64, val='filterSelection.cpucores', unit='', class='cpu-cores')
  slider-filter(title='Storage', min=0.25 ,step=0.25, max=640, val='filterSelection.storage', unit = 'Gb', class='storage')
  slider-filter(title='Maximum simultaneous jobs', min=1, step=1, max=64, val='filterSelection.simultaneousjobs', unit='', class='maximum-sim-jobs')
  check-box-filter(title='DataBases', selections='filterSelection.servicesSelections')
  check-box-filter(title='Location', selections='filterSelection.countrySelections')
  check-box-filter(title='Support 24/7', selections='filterSelection.supportSelections')
  check-box-filter(title='Security Scanning', selections='filterSelection.securitySelections')
