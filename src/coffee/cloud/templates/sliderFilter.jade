.slider-filte
  h3 {{ title }}
  hr(class="viiva")
  p {{ min }}{{ unit }} {{ max }}{{ unit }}
  input.slider-slider(type="range" name="points" min="{{ min }}" step="{{ step }}" max="{{ max }}" ng-model="val")
  p {{ val }}{{ unit }}
